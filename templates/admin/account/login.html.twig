<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Connexion administration</title>
    <link href="https://netdna.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css" rel="stylesheet"> 
    <link rel="stylesheet" href="/css/bootstrap.min.css">
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/css/admin.css">
</head>
<body>
    <main class="container">
        <div class="row">
            <div class="col-md-6 mx-auto">
                <div class="card bg-light">
                    <div class="card-header">
                        <h1 class="text-center">Booking</h1>
                        <h4 class="card-title text-center"><i class="fa fa-lock"></i> Connexion à l'administration</h4>
                    </div>
                </div>
                <div class="card-body bg-light">
                    {% if hasError %}
                        <div class="alert alert-warning">
                            <p>Les informations fournies sont erronées.</p>
                        </div>
                    {% endif %}
                    <form method="POST">
                        <div class="form-group">
                            <label for="username">Email</label>
                            <input type="text" name="_username" id="username" placeholder="Votre email" class="form-control" value="{{username}}">
                        </div>
                        <div class="form-group">
                            <label for="password">Votre mot de passe</label>
                            <input type="password" name="_password" id="password" placeholder="Votre mot de passe" class="form-control">
                        </div>
                        <div class="form-group">
                            <button type="submit" class="btn btn-primary"><i class="fa fa-unlock"></i> Connexion</button>
                            <a href="{{path('homepage')}}" class="btn btn-link bg-secondary">Retour sur le site</a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </main>
</body>
</html>
